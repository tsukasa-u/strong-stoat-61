# Vue SSR Example

Vue SSR project entry using official-style Single File Component (`App.vue`).

This sample is SSR-only.
It demonstrates obfuscation on the server-rendered HTML response and does not hydrate client-side state.

## Run

```bash
pnpm install
pnpm dev
```
