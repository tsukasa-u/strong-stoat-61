# React SSR Example

Minimal React SSR project entry.

Supported pages: `/`, `/pre-encoded`

Numeric counter demos are intentionally omitted. Use string-based state or server-issued obfuscated transitions for protected dynamic values.

## Run

```bash
pnpm install
pnpm dev
```
